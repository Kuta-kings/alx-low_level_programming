#include <stdio.h>
/**
 * main - A program that runs numbers less than ten in base ten
 * Return: 0 (Seuccess)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);

	printf("\n");

	return (0);
}
